<!DOCTYPE html>
<html lang="en">

<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <% if is_building == false %>
    <meta name="viewport" content="width=device-width">
    <% end %>
    <title><%= data.page.title || "Email Template" %></title>

    <%= inline_stylesheet 'reset' %>
    <%= inline_stylesheet 'all' %>
    <%= stylesheet_link_tag 'premail' %>
</head>

<body>
    <table id="backgroundTable" width="100%" style="background:#fff;">
        <tr>
            <td class="body" align="center" valign="top" style="background:#fff;" width="100%">
                <table>
                    <tr>
                        <td class="main" width="600" align="center">
                            <%= yield %>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>

    <!-- Exact Target tracking code -->
    <custom name="opencounter" type="tracking">
</body>
</html>
