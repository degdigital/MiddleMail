default:
    - "env:build"
    - "shell:build"
    - "emailBuilder:build"
    - "string-replace:build"
    - "clean:build"
server:
    - "shell:server"
preview:
    - 'env:preview'
    - 'shell:build'
    - 'emailBuilder:preview'
    - 'clean:preview'